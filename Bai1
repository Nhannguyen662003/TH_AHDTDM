#include <stdio.h>

// Hàm kiểm tra số n có là bội của 7 hay không
int isDivisibleBy7(int n) {
  return (n % 7 == 0);
}

// Hàm in ra các số nguyên có 2 chữ số và là bội của 7
void printBoidsOf7() {
  for (int i = 10; i < 100; i++) {
    if (isDivisibleBy7(i)) {
      printf("%d ", i);
    }
  }
}

int main() {
  printBoidsOf7();
  return 0;
}
